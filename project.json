{
	"watch": {
		"rules": {
			"core": {
				"files": ["core/*"],
				"tasks": "npm run build --prefix core"
			},
			"demo": {
				"files": ["demo/*"],
				"tasks": "npm run build --prefix demo"
			}
		}
	}
}
